# Проверка переноса в saypix

Дата: 2026-09-12. База: `ae2d3c0` (`origin/main`).

- `npm ci` и `npm test` в корне сайта прошли.
- `python3 brand/scripts/qa-svg.py`: 17 SVG, исходные path сохранены.
- `node brand/scripts/qa-assets.cjs`: 65 PNG, 14 сравнений оптимизации,
  ICO и crop QA прошли. Использованы установленные Sharp/SVGO из среды подготовки.
- SHA-256 locked master:
  `fe0dde1281b8fe1b4a8a44e54365050becd72e2c8ea21acde61e5f28fc12be31`.
- `git diff --check` прошёл.

`git diff --exit-code -- dist` после сборки не прошёл: уже в базовом `main`
сохранённый `dist/` не совпадает с результатом сборки текущих исходников.
Файлы сайта не менялись при переносе бренда. Пересобранный `dist/` не включён
в коммит, чтобы не объединять исправление состояния сайта с импортом бренда.
Проверка CI на соответствие `dist/` остаётся известным ограничением этой базы.

Browser/PDF-отчёты сохранены из предыдущей сессии; повторных browser/PDF тестов
при переносе не выполнялось. Генерированный макет поискового результата
не включён в канонические файлы бренда. Публикация на действующий сайт не выполнена.
